import React from 'react'
import Loading from '..'

export default function LoadingData() {
    return (
        <div className="m-auto h-full grid justify-center items-center">
             <Loading color={"black"} size={80} />
        </div>
    )
}
